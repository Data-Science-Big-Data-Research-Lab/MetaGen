# Runtime dependencies. These are the ones pyproject.toml declares as dependencies;
# installing the package brings them along, so this file is for a bare checkout.
numpy>=1.21.0
scipy>=1.7.0

# Running the test suite. Also available as the "test" extra:
#     pip install -e .[test]
pytest

# Building the documentation.
sphinx-rtd-theme
